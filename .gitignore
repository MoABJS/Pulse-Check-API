# Dependencies — never commit these, npm install regenerates them
node_modules/

# Compiled TypeScript output — regenerated by npm run build
dist/

# Environment variables — could contain secrets
.env

# Mac clutter
.DS_Store